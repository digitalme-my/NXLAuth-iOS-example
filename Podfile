target 'NXLAuthExample' do
  platform :ios, '9.0'

  # AppAuth Pod
  # In production, just use `pod 'AppAuth'` without the path reference.
  pod 'AppAuth', :path => '../AppAuth-iOS/'
  pod 'JWT'
  # pod 'AppAuth', :git => 'https://github.com/jasonleewj85/AppAuth-iOS.git'
end
